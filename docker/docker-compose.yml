services:
  postgres:
    image: postgres:16
    container_name: governai-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: governai
    ports:
      - "5432:5432"
    volumes:
      - governai_pgdata:/var/lib/postgresql/data

volumes:
  governai_pgdata: